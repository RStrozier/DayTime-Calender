let fruit =['banana', 'apple', 'orange', 'tomato', 'grape']

for (let index = 0; index < fruit.length; index++) {
    console.log(fruit[index])
}

function add(a, b, c) {
    var addTo= (a + b + c);
    return addTo;
};
add(2, 3, 4);
